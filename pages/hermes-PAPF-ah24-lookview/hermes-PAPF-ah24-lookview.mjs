

window.addEventListener('DOMContentLoaded', () => {
    // const imagesLineUp = document.querySelectorAll('.container-lineup img')
    // imagesLineUp.forEach((img, key) => {
    //     img.src = `https://source.unsplash.com/random?sig=${key}`;
    //   });
})